'use strict'

// adds another input field to poll creation
function addInput() {
  var pollNameWrapper = document.getElementById('poll-name-wrapper');
  console.log("Add a field under " + pollNameWrapper)
}
