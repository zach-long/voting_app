<% include components/header.ejs %>

  <div class="main-content">
    <h3>View all polls</h3>
    <% polls.forEach(function(poll) { %>
      <%- include('components/openPolls', { poll: poll }); %>
    <% }); %>
  </div>

<% include components/footer.ejs %>
