<% include components/header.ejs %>

  <a href="/u" class="back-button"><i class="fa fa-arrow-left" aria-hidden="true"></i> Go back</a>

  <div class="main-content">
    <% if (typeof poll != 'undefined') { %>
      <%- include('partials/editPoll', {poll: poll}); %>
    <% } else { %>
      <%- include('partials/createPoll'); %>
    <% }; %>
  </div>

<% include components/footer.ejs %>
